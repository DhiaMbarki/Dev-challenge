import React from 'react'

const Home = () => {
    return(
        <section className = "hero">
            <nav>
                <h2>Welcome</h2>
                <button>Logout</button>
            </nav>

        </section>
    )
}

export default Home ;